/**
 * `initialState`
 */

const initialState = {
  isLoadingData: false,
  product: {},
  productList: [],
  products: [],
  searchQuery: {}
};

/**
 * Export `initialState`.
 */

export default initialState;
