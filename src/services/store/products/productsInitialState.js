/**
 * `initialState`
 */

const initialState = {
  isLoadingData: false,
  product: {},
  productList: [],
  productListId: {},
  products: [],
  searchQuery: {}
};

/**
 * Export `initialState`.
 */

export default initialState;
